# 2026-05-14

- 排查 `generate_sidebar.py` 挂起：旧脚本把完整 README 传给 Claude CLI，prompt 过长，`claude -p` 120 秒超时；日志又因为 Python 缓冲看起来像静默挂死。
- 修复侧栏生成：只抽取 README 的 `旅行规划 (sidebar.html)` 段落传给 Claude，增加日志 flush，Claude 超时或返回非 HTML 时写入交通评估版 fallback HTML。
- `user_start.sh` 增加 `PYTHONUNBUFFERED=1`，让后台 Python 日志实时写入。
